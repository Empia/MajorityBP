package main.scala.MM
import scala.collection.mutable._
import main.scala._

class Subject(title: String) {
  //var subj_elems: ListBuffer[Either[BProcess, BehavElement]] = ListBuffer()
  var subj_elems: ListBuffer[BProcess] = ListBuffer()
}